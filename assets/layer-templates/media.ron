// Media layer: decoded media frames in layer-local time
// (REQ-LAYER-008: In → Media(asset_id) → Out).
// The display name stays "Video": the user-visible label of
// CommandId::LayerAddVideo is kept even though the node type is `media`.
LayerTemplate(
    key: "media",
    display_name: "Video",
    nodes: [
        TemplateNode(
            key: "in",
            type_key: "net.in",
            label: Some("In"),
            position: (-360.0, 0.0),
            outputs: [
                OutputPort(name: "base_geometry", data_type: DataTypeId(50)),
                OutputPort(name: "t", data_type: DataTypeId(10)),
                OutputPort(name: "f", data_type: DataTypeId(10)),
            ],
        ),
        TemplateNode(
            key: "media",
            type_key: "media",
            position: (-120.0, 0.0),
        ),
        TemplateNode(
            key: "out",
            type_key: "net.out",
            label: Some("Out"),
            position: (120.0, 0.0),
            inputs: [
                InputPort(name: "frame", accepted_types: [DataTypeId(1)]),
            ],
        ),
    ],
    edges: [
        TemplateEdge(from: ("media", "frame"), to: ("out", "frame")),
    ],
)
