# TASK-034: プリセット/テンプレートシステム
- **マイルストーン**: MS7 Polish
- **関連要件**: REQ-PLUGIN-005
- **規模**: M
- **依存タスク**: TASK-015, TASK-008

## 概要
ノードプリセット保存/読み込み（パラメータスナップショット）、サブグラフテンプレート保存/エクスポート（公開パラメータ付き）、プロジェクトテンプレートシステム（テンプレートからの新規プロジェクト作成）、テンプレートブラウザUI、インポート/エクスポートワークフロー（zipパッケージ形式、manifest.toml）、バンドル済みスターターテンプレート（SNS縦型動画、MV、プレゼンテーション）を実装する。

## 実装ステップ
1. ノードプリセット保存/読み込み実装（パラメータスナップショット）
2. サブグラフテンプレート保存/エクスポート実装（公開パラメータ付き）
3. プロジェクトテンプレートシステム実装（テンプレートからの新規プロジェクト作成）
4. テンプレートブラウザUI実装
5. インポート/エクスポートワークフロー実装（zipパッケージ形式、manifest.toml）
6. バンドル済みスターターテンプレート作成（SNS縦型動画、MV、プレゼンテーション）

## 対象コンポーネント
- `crates/ravel-core/src/preset/` (プリセットシステム)
- `crates/ravel-core/src/template/` (テンプレートシステム)
- `crates/ravel-ui/src/panels/template_browser/` (テンプレートブラウザUI)
- `crates/ravel-core/src/io/package/` (パッケージインポート/エクスポート)

## 完了条件
- [ ] ノードプリセットが保存・読み込みできる
- [ ] サブグラフテンプレートが公開パラメータ付きで保存・エクスポートできる
- [ ] テンプレートから新規プロジェクトが作成できる
- [ ] テンプレートブラウザUIが動作する
- [ ] zipパッケージ形式でのインポート/エクスポートが動作する
- [ ] SNS縦型動画・MV・プレゼンテーションのスターターテンプレートがバンドルされている
