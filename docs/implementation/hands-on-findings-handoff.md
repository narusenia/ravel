# 実機フィードバックの棚卸し — 次セッションへの引き継ぎ

> **Status**: 起票分の 8 件と `NGR-1`〜`NGR-3` を実装済み、未決の設計判断は
> 全部決着。残るバグは 2 件で、うち 1 件は計画書へ移した — 2026-08-09

**現在地は「次に何をするか」節が正。** その下の「さばいた結果」以降は
2026-08-08 の棚卸しの記録で、当時の事実として残してある。

2026-08-08 の実機セッションで挙がった 47 件を全部さばいた記録。
**この文書は棚卸しの結果と着手順だけを持つ。** 各項目の中身は
`issues/` と各計画書が正で、ここには書かない。

## さばいた結果

| 区分 | 件数 | 行き先 |
|---|---|---|
| 即修正（マージ済み） | 7 | PR #337 |
| バグとして起票 | 9 | `issues/`（下表） |
| 既存項目でカバー済み | 4 | — |
| 機能要望 | 23 | `backlog.md`「実機フィードバック由来の機能要望」 |
| 設計ゲート → 計画書 | 3 | `discrete-keyframes-plan.md` / `asset-identity-plan.md` / `parameter-groups-plan.md` |
| 調べたら既に実装済み | 1 | 連番読み込み（`image_seq::detect_sequence`） |

### 起票したバグ

| ID | 内容 | 重さ |
|---|---|---|
| [`HIGH-27`](../../issues/closed/HIGH-27-timeline-keyframes-invisible-inside-subnets.md) | Subnet に畳むとキーフレームが Timeline から消えるのに評価は続く | 文書と UI が食い違う |
| [`HIGH-28`](../../issues/closed/HIGH-28-scrub-commit-lost-when-properties-rebuilds.md) | スクラブ中の再構築で `Commit` が失われ undo が効かない | 編集が取り消せない |
| [`HIGH-29`](../../issues/closed/HIGH-29-no-menu-bar-outside-macos.md) | Windows / Linux にメニューが 1 つも出ない | **リリース阻害** |
| `MED-APP-26` | 「プロジェクトへ露出」のトグルが片道 | 解除不能 |
| `MED-APP-27` | Tab の検索パレットがキャンバス中央固定 | — |
| `MED-APP-28` | Timeline のバードラッグが複数選択を無視 | — |
| `MED-APP-29` | `layer.ref` が数値スクラブ、出力型が `port` に追随しない | 型が嘘をつく |
| `MED-APP-30` | ラバーバンド中に Properties が作り直され続ける | 一部未確認 |
| `LOW-APP-23` | 評価時間の表示がノード直下で下のノードに被る | — |

## 着手順（提案）

> **この節は消化済み**（1〜3 は #343 / #344 / #346 で閉じた）。
> 当時どう並べてなぜそう並べたかの記録として残す。
> **今どこにいるかは「次に何をするか」節**。

**この順は roadmap を置き換えない。** 上記の項目に限った並びで、
根拠は「リリースを塞ぐか」「他の修正に相乗りできるか」の 2 つ。

### 1. `HIGH-29`（Windows のメニュー）

**唯一のリリース阻害。** 他の何とも依存が無く、単独で着手できる。
Windows 実機での確認が要るので、実機を触れるときに回す。

### 2. `HIGH-28` + `LOW-APP-07` + `MED-APP-30` をまとめて

**3 つとも「保留中のコミットを、それを運ぶ入れ物ごと捨てている」**という
同じ欠陥。別々に直すと同じ穴が残る側が出る。

- `HIGH-28` — スクラブの `Commit`
- `LOW-APP-07` — デバウンスされた色のコミット
- `MED-APP-30` — 同じ再構築経路が毎マウス移動で走る

`MED-APP-30` は着手前に「バンドが毎 move で選択を公開しているか」を
確かめること（起票時に特定できていない）。

### 3. `HIGH-27`（Subnet のキーフレーム）

**`MED-APP-25` を先に直すか同時に直す。** `MED-APP-25`（Subnet のコピペが
内部 `NodeId` を複製しない）が残っていると、行を bare `NodeId` で
アドレスする前提が崩れる。理由は `HIGH-27` の個票に書いた。

### 4. 残りの medium / low

`MED-APP-26` は分岐 1 つ、`MED-APP-27` は位置を渡すだけで、どちらも小さい。
`MED-APP-29` は `NETIF-2` の型の文脈依存に乗るので、そこを読んでから。

## 設計ゲートの 3 件について

計画書は書けている（`backlog.md` に単位も登録済み）。着手可能なのは
`DISK-1` / `AID-1` / `PGRP-1` / `PGRP-5`。

**フォーマット版は逐次採番**と決めた。3 件（+ `CM-2`）が `v8` を取り合うので、
着手時に `manifest.rs` の `CURRENT_FORMAT_VERSION` を見て**その時点の次**を取る。
計画書に版番号を焼き込んでいないのはこのため。

## grill で決まったこと（2026-08-08）

上の棚卸しの後、対話で 12 件の設計判断が確定し、**機能要望 23 件のうち
18 件に計画が付いた**。判断の中身は各計画書が正。ここは行き先だけを持つ。

| 決まったこと | 行き先 |
|---|---|
| UX の洗い出しは「ワークフロー貫通」で行う（パネルごとの棚卸しをもう 1 枚作らない） | `refactor-plan-0808.md` |
| 問題の基準は「往復回数」と「探し直しの回数」— 数えられる形にする | 同上 |
| 測るのはユーザー、計器（所在表・シナリオ・集計表）を作るのは実装側 | 同上 `UX-1`〜`UX-3` |
| シナリオは 2 本（素材→レイヤー→キー→書き出し / ノードでプロシージャル） | 同上 `UX-2` |
| 既知分を先に単位化し、測定が遅れても実装は止めない | 同上 `UX-4`〜`UX-12` |
| 自動整列は手動コマンドで undo に乗る。自動では走らせない | `node-graph-readability-plan.md` `NGR-1` / `NGR-2` |
| 上→下フローは表示の設定。保存形式は変えない | 同上 `NGR-5` |
| エッジスタイルとフロー方向は `SettingsLayer` の `node_editor` 節へ | 同上 `NGR-3` / `NGR-5` |
| Wrangle は複数文・複数属性の CPU ノード 1 つ。GPU も要素の増減も含めない | `wrangle-plan.md` `WRG-1`〜`WRG-3` |
| カスタムスライダーは任意ノードの spare parameter に一般化する（機構を 2 本作らない） | 同上 `WRG-4`〜`WRG-6` |
| `HIGH-30` を先に直す（カスタムパラメータを持てるノードが増えるほど穴が広がる） | `roadmap.md` フェーズ UX |
| リリース前 / 後の線引き | `roadmap.md` フェーズ UX とその「リリース後へ送るもの」 |

計画が付かなかった 5 件（Vec の broadcast、Vec の全軸同時操作、
`field.apply` の補完、Noise の evolution / flow、ベジエ Ellipse / Arc）は
`backlog.md` の「まだ計画の無いもの」に残っている。

**調査で 2 つ、認識が更新された。** どちらも計画書に根拠付きで書いた。

- Wrangle に `PLUG-3`（WGSL シェーダノード）は要らない。
  `field.expression` と `attribute.set` で単一属性の Wrangle 相当は
  既に組めており、足りないのは言語の側面だけ
- `EdgeStyle` はどこにも保存されておらず、パネルを作り直すたびに黙って
  既定へ戻る。`node_editor` 設定節がこの取りこぼしも塞ぐ

## 次に何をするか（2026-08-08 更新）

### 片付いたもの

| PR | 内容 |
|---|---|
| #337 | 即修正 7 件（a11y 抑制 / CLI の `~` / `math.scalar` の `a` / Dialog 位置 / `Cmd+D` / Ports のガター / 式欄の monospace） |
| #343 | `HIGH-29` 非 macOS のアプリメニューバー |
| #344 | `HIGH-28` + `LOW-APP-07` + `MED-APP-30` ジェスチャのコミット消失 |
| #345 | 計画書 3 本（`refactor-plan-0808` / `node-graph-readability-plan` / `wrangle-plan`） |
| #346 | `MED-APP-25` + `HIGH-30` + `HIGH-27` Subnet の同一性とピン同期 |
| #347 | `NGR-1`〜`NGR-3` ノードの自動整列（`L`）と `node_editor` 設定節 |
| #348 | 未決 4 件の決着 + `MED-APP-26` / `27` / `28` / `LOW-APP-24` |
| #349 | `MED-APP-31` + `MED-APP-29` の計画書化 |

**着手順を 1〜4 まで消化した**（4 は残りの medium / low）。起票したバグ 9 件の
うち 7 件が閉じ、調査の過程で新しく起票した `HIGH-30` / `MED-APP-31` /
`LOW-APP-24` の 3 件も閉じた。**リリース阻害はゼロ、未決の設計判断もゼロ。**

テストは 2814 → 2878 → 2888。

### 残っている実機由来のバグ

| ID | 内容 | 備考 |
|---|---|---|
| `MED-APP-29` | `layer.ref` が数値スクラブ、出力型が追随しない | **計画書へ移した** — `contextual-parameter-options-plan.md` の `CPO-1`〜`CPO-7`。1 ノードの直しではなく機構が要ると分かったため |
| `LOW-APP-23` | 評価時間の表示が下のノードに被る | 見送り（2026-08-09 のユーザー判断） |

`MED-APP-31` は #349 で閉じた。**個票の診断は半分だけ当たっていた** — 矢印は
登録順どおりだったが、Escape は `DropdownMenu` が再オープン時に `PopupMenu` を
focus し直さないという別の欠陥で、直したのは gpui-component のフォーク側。
詳細は `closed/medium-app-shell.md` の同項目。

**`MED-APP-28` は個票の修正方針だけでは直らなかった。** バーの mousedown が
ドラッグ開始前に選択を 1 枚へ潰しており、押下側も直して初めて選択全体が動く。
詳細は `closed/medium-app-shell.md` の同項目。

### 計画書の単位

**リリース前**（`roadmap.md` の「フェーズ UX」）:

- `UX-1`〜`UX-3` — ワークフロー貫通の計器と測定の取り込み。
  **`UX-2` の測定はユーザーが実機で通す工程**で、ここは委譲できない
- `UX-4`〜`UX-12` — 既知分の実装。**`UX-1`〜`UX-3` を待たない**
- ~~`NGR-1`〜`NGR-3`~~ — 自動整列と `node_editor` 設定節（**#347 で完了**）

**リリース後**: `NGR-4`〜`NGR-7`、`WRG-1`〜`WRG-7`、
`DISK-*` / `AID-*` / `PGRP-*`。

## 判断を保留したもの

- **`MED-APP-30` の因果**（バンドの公開頻度）— 未確認と個票に明記した
- **`HIGH-29` の Linux** — Windows は実機で確認して閉じたが、**Linux は実機が
  無く未確認のまま**。`set_menus` の実装が無いのはソース上確かで、描画先も
  Windows と同じ 1 経路なので同じ結果になる見込み（`closed/` の個票に残した）
- **CI の `bench` ジョブのヒット率 0.00% と Windows の 91.90%** — どちらも
  warm 1 サンプルしか無く、systematic か判断できない
  （`ci-cache-plan.md` の「追って見るもの」）

### ユーザーに聞くべきだったこと（2026-08-09 に 4 件とも決着）

**未決はゼロ。** 判断の中身は各文書が正で、ここは結論と行き先だけを持つ。

1. **spare parameter は既存の露出モデルに乗る — 案 A（2 段）。**
   生やすのと露出するのは別の操作にする。理由は「宣言経路を 2 本にしない」で、
   案 B の得（操作 1 段）は後から 1 手足せば取り戻せるが、損（`EXPO-3` と
   CLI の `--param` が 2 経路を見る）は入れた後では戻せない。
   → `wrangle-plan.md`「決着」節。**`WRG-4` に残る着手条件は無くなった**
2. **`issues/` の UI 系は `UX-*` に取り込まない。** `MED-APP-27` は
   どちらにせよ数行で `UX-*` を待つ理由が無く、`MED-APP-20` は
   `vector-field-plan.md` 単位 5 が入るまで画面に到達しない。
   取り込むと `roadmap.md` のクラスタ組み替えが要るだけで得るものが無い
3. **ループ範囲は `ui_state.json`。** AE と違いプロジェクトには付いていかない。
   **付いてこなくて困る実例が出てから `.ravprj` へ移す**（移すこと自体が
   フォーマット版 1 つなので、先送りしても取り合いは無くならない）。
   → `refactor-plan-0808.md` 単位 9
4. **`HIGH-29` は閉じた。** Windows 実機でメニューバーの表示と操作を確認。
   **Linux は実機が無いので未確認のまま**、`closed/` の個票に残してある

## 踏んだ罠（繰り返さない）

このセッションで実際に踏んで時間を失ったもの。

1. **`rg -rn` は `--replace=n`。** `grep` の癖で打つとマッチが全部 `n` に
   置換されて表示され、**存在しない API 名を読み取る**
   （`PopupMenuItem` を `nItem` だと思い込んだ）。行番号だけなら `-n` 単独
2. **`sccache` が直接 `cargo` 起動時だけ落ちる**
   （`Operation not permitted`）。`mise run check` と lefthook 経由は通る。
   原因未特定、回避は `RUSTC_WRAPPER=`
3. **rustfmt の整形差分で `git commit` が黙って落ちる。**
   終了コードを必ず見る。さらに**失敗したコミットはステージを残す**ので、
   次のコミットに無関係なファイルが混入する
4. **個票を `closed/` へ移すとリンクが切れる。** 件数だけ合わせても
   `docs:check` は通らない。他の文書からの参照を張り替える
5. **委譲中の worktree に呼び出し側が git 操作をしない。**
   検証のつもりで `git rebase` を走らせるとエージェントの足元でコミットが
   書き換わる
6. **変異検査の復元に `git checkout <file>` を使わない。**
   未コミットの修正本体ごと巻き戻る。コミット後に行うか、パッチを退避する

## レビューの効き方（このセッションの実測）

- **独立レビュー（codex）が 4 単位すべてで、実装者と自分が通していた欠陥を
  捕まえた。** ただし**提案された直し方をそのまま採ったのは 2 回だけ**で、
  残りは観察だけ受けて別の直し方にした（`HIGH-28` の
  `rebuild_subnets` を `None` で中断する案は「編集が黙って消える」に
  変わるので却下）
- **「Design gate の実装計画が要る」は 4 回指摘され 4 回却下した。**
  基準は「複数クレート / 複数パネル / サブシステムの作り直し」で、
  既存経路を消費するだけの変更は当たらない
- **変異検査を毎回させたのが効いた。** とくに `HIGH-30` の
  「ポート名リネームは残しキー移動だけ無効化」は、**エッジは直ったが
  キーフレームだけ静かに死ぬ**という部分修正が実在することを示した
- **`HIGH-29` では独立レビューを追ううちに、gpui-component が
  `AppMenuBar` を持っていることが判明**して手書き 136 行が消えた。
  指摘そのものより、指摘を追う過程で見つかったものが大きかった
