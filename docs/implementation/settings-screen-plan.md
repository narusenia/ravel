# 設定画面と設定の適用 実装計画（REQ-PROJ-004）

> **Status**: 計画のみ — 2026-07-29

対象要件: REQ-PROJ-004（カテゴリ別設定 + プロジェクトオーバーライド）。
関連: REQ-PROJ-002（自動保存間隔を設定 UI で）、REQ-UI-006（テーマシステム +
アクセシビリティ）、REQ-UI-007（フルカスタマイズキーバインド）、
REQ-CORE-006（キャッシュ設定）、REQ-UI-013（ワークスペース）。

REQ-UI-006 の受入条件のうち本計画が満たすのは「ダーク/ライト切り替え」と
「カスタムテーマの読み込み」の 2 つ。「色覚多様性テーマ」「UI スケーリング」
「アニメーション削減モード」は下記のゲート付き単位に置く（前者 2 つは
テーマ資産とパネル側の対応が前提で、設定画面の有無とは別の作業）。

回収する issue: `MED-APP-10`（設定レイヤー全体が永続化されるが一切適用
されない — 日本語ロケールが到達不能）、`LOW-APP-15`（ユーザーのキーバインド
カスタマイズが読み込めない）。

## 問題

### 1. 設定は「保存できるが効かない」

4 層マージ（`default → global → project → user`）は実装済みで
（`ravel-app/src/project/settings.rs:105`、`:154`）、TOML の往復もテストされて
いる。だが **`ResolvedSettings` を消費している実行時コードが 1 つも無い** —
唯一の呼び出し元は `project/mod.rs:319-332` で、そこから先はテストしか読んで
いない。具体的には:

| 設定 | 保存 | 適用 |
|---|---|---|
| `locale` | ✅ | ❌ `main.rs:49` が `"en"` 固定で初期化。**日本語ロケールが到達不能** |
| `auto_save.enabled` / `interval_seconds` | ✅ | ❌ タイマーの実装自体が無い |
| `playback.frame_rate` | ✅ | ❌ 新規コンプの既定に繋がっていない |
| `playback.proxy_mode` / `proxy_resolution` | ✅ | ❌ プロキシ生成が未実装 |
| `color.ocio_config` / `working_space` / `display_space` | ✅ | ❌ カラー管理が未実装 |

`assets/locales` に日本語があり `ravel_i18n::set_locale()` も存在するのに、
**ユーザーがそこへ到達する経路が無い**のが現状。

### 2. 設定 UI が無く、要件は「全設定を UI で変更できる」を要求している

コマンドテーブルの settings 系は `composition.settings`（コンプ設定）だけ。
REQ-PROJ-004 の受入条件は「設定 UI で全設定を変更できる」だが、上表の
とおり**大半の設定は変更しても何も起きない**。UI を先に全項目出すと、
`MED-APP-10` を UI 層に転写するだけになる。

### 3. キーバインドはパーサーがユーザーファイルを読める形なのに読んでいない

`ravel-ui/src/keybindings/parser.rs:71-146` は TOML / JSON を扱えるが、
`main.rs:70` は `AppShell::default()` 経由で埋め込みの
`assets/keybindings/default.toml` しか読まない（`LOW-APP-15`）。

## 決定事項

### 「出す項目 = 実際に効く項目」を規約にする

**設定画面に項目を出す条件は、その設定が挙動に反映されること。** 効かない
項目は画面に出さない（コメントアウトでもグレーアウトでもなく、存在させない）。

- 受入条件は UI ではなく**挙動**で書く。「言語を切り替えると次の描画で
  UI 文字列が変わる」であって「言語の dropdown がある」ではない。
- 機能が未実装の設定（プロキシ、カラー管理）は**画面に出さず**、要件側に
  「未対応」と明記する。機能が入った時点で項目を足す（下記のゲート付き単位）。
- この規約が `MED-APP-10` の再発防止そのものになる。

### `gpui_component::setting::Settings` を使う

自前でフォームを組まない。同コンポーネントは
`Settings → SettingPage → SettingGroup → SettingItem → SettingField` の
階層とサイドバーを持ち、フィールドは switch / checkbox / input / dropdown /
scrollable_dropdown / number_input / 任意 element を備える。

層モデルに使うのは **`on_reset(is_dirty, reset)` の方だけ**:

- `is_dirty` = **編集中の層に明示値がある**（マージ後の値が既定と違う、では
  ない。層に値があるかどうか）
- `reset` = **その層からその値を消す**（下位層の値へ戻る）

**`default_value()` は使わない。** あれは「静的な既定値を書き戻す」reset
機構（`fields/mod.rs` のコメントが "this value can be used to reset the
setting to its default state" と書いている）で、層モデルでは**上書きを消す
のではなく既定値と同じ明示値を作ってしまう**。`on_reset` は
`default_value` ベースの reset より優先されると明記されているので、全
フィールドで `on_reset` を付ける。

これで「この項目はこの層で上書きしている」の表示と解除がコンポーネントの
標準機能で成立し、独自のバッジ UI を作らずに済む。

### モーダルダイアログにする

`window.open_dialog` の前例が 2 箇所（`ravel-app/src/workspace.rs:793`、
`:1026`）あり、フォームの前例は `composition_form.rs`（345 行）。

- 独立ウィンドウにしない理由は、**分離ウィンドウの既知負債
  （`MED-APP-01`: OS ウィンドウを閉じるとシェルが desync、`reattach_window` が
  dead API）に触れないため**。設定画面のためにあの負債を先に直す価値はない。
- ドック可能パネルにしない理由は、常時見るものではないのに
  パネル登録とワークスペースプリセット資産の全更新が付いてくるため
  （パネル配置の再設計は `done/free-pane-docking-plan.md` に移った）。
- **将来方向**: カラー管理が入って Viewer と見比べる必要が出たら独立
  ウィンドウ化を検討する。その時点で `MED-APP-01` が前提になる。

### 環境設定とプロジェクト設定の 2 画面に分ける（AE 型）

| 画面 | 書き込み先 | 内容 |
|---|---|---|
| 環境設定（Preferences） | **`global` 層**（`<config_base>/ravel/settings.toml`） | 外観、言語、キーバインド、キャッシュ、自動保存 |
| プロジェクト設定 | **`project` 層**（`.ravprj` の `settings.toml`） | 既定フレームレート、将来のカラースペース |

**書き込み先が画面で決まる**ので、行ごとに層を選ぶ UI が要らず、誤って
グローバルへ書く事故も起きない。

**層の対応はこの向きでないと壊れる。** 解決順は
`default → global → project → user`（後ろが強い。`project/mod.rs:319-332`）なので、
環境設定を最上位の `user` 層に書くと**プロジェクト設定が環境設定に負ける** —
REQ-PROJ-004 の「プロジェクト固有設定でグローバル設定を上書きできる」が
成立しなくなる。`global` こそが per-user のグローバル設定
（`project/paths.rs:6-7`「user-wide preferences survive across projects」）で、
これに書けば `project` が正しく上書きする。

`user` 層は**現在ファイルの置き場も呼び出し元も無い**（`resolved_settings` の
第 2 引数にテストしか値を渡していない）。本計画では触らず、「マシン固有の
最終上書き」「CLI からの一時上書き」用の枠として空けておく。

### 即時適用。undo には載せない

- 変更した瞬間に反映し、ダイアログは閉じるボタンのみ。
- **設定は `Document` ではないので undo に載せない。** 載せると undo 履歴が
  「編集の履歴」でなくなる。
- 「既定に戻す」はコンポーネントの `on_reset` を使う（層の値を消す）。
- 永続化は変更ごとに TOML を書く。書き込み失敗は**無言にしない**
  （`CRIT-02` と同じ失敗形を作らないため、通知を出す）。

### 適用経路は 1 本にする

設定の適用は `ResolvedSettings` を配る 1 経路に限る。

- 解決済み設定は**耐久状態**なので Global に置いてよい（`.agents/rules/gpui.md`
  の「新 Global は耐久状態のみ」に合致）。ダイアログは設定を書き、Global を
  更新し、購読側が反応する。**ダイアログが各機能を直接叩かない。**
- 設定ダイアログを開くのは `CommandId` + `for_each_command!` テーブル経由
  （`AppPreferences` / `ProjectSettings` を追加）。メニューは File または Edit
  の慣例に合わせ、`menu.edit` へ入れる（macOS のアプリメニュー慣例との整合は
  `native_menu` 対応時に再検討）。
- `render()` の中で設定を書かない。ロケール変更後の再描画は、購読側が
  `cx.notify()` する形にする（render 純粋性）。

### 設定ファイルは単一 `settings.toml` のまま。要件を改訂する

REQ-PROJ-004 は `general` / `appearance` / `keybindings` / `performance` /
`plugins` の 5 ファイル構成を指定しているが、**4 層マージが既に単一ファイルで
動いている**ので、分割は読み込み先を 4×5 に増やすだけで得が無い（項目は
まだ十数件）。

- 単一 `settings.toml` を正とし、要件を改訂する。
- **キーバインドだけは例外**で、既に別資産（`assets/keybindings/default.toml`）
  になっている。ユーザー上書きも `<config>/keybindings.toml` として別ファイル
  で読む。
- 項目数がファイルとして扱いにくい規模に育ったら分割を再検討する（判断は
  先送りではなく「今は単一が正」と決めた上での将来方向）。

## 目標構成

```text
Edit ▸ Preferences…                    Composition ▸ Project Settings…
        │                                       │
        ▼                                       ▼
┌──────────────────────────┐        ┌──────────────────────────┐
│ 環境設定（global 層へ書く）│        │ プロジェクト設定           │
│ ┌──────┬───────────────┐ │        │ （project 層へ書く）       │
│ │外観  │ テーマモード   │ │        │  既定フレームレート        │
│ │言語  │ テーマ         │ │        │  （将来: カラースペース）   │
│ │キー  │ 言語           │ │        └──────────────────────────┘
│ │      │ …              │ │
│ └──────┴───────────────┘ │        どちらも即時適用・undo 対象外
└──────────────────────────┘        「既定に戻す」= その層の値を消す
        │
        ▼
  ResolvedSettings（Global。耐久状態）
        │
        ├→ ravel_i18n::set_locale() → 全パネル再描画
        ├→ gpui_component::Theme
        └→ （機能が入り次第）キャッシュ予算 / 自動保存 / …
```

## 実装単位

### 単位 1 (`SET-1`): 設定の適用経路と言語（UI なし）

**UI より先に「効く」を作る。** これだけで `MED-APP-10` の中核が消える。

- 起動時に 4 層を解決し、`ResolvedSettings` を Global として配る。
- `locale` を `ravel_i18n::set_locale()` に適用（`main.rs:49` の `"en"` 固定を
  解決値に差し替え）。未知のロケールは警告して既定へフォールバック。
- 設定の書き込み API（`global` 層 / `project` 層への 1 項目更新 + 保存）。
  保存失敗は通知（無言にしない）。

**完了条件**

- `settings.toml` に `locale = "ja"` がある状態で起動すると UI が日本語に
  なるヘッドレステスト。
- 未知のロケールで警告 + 既定フォールバックのテスト。
- 層を跨いだ解決結果が既存の `resolved_settings_layers_correctly` と一致
  （挙動不変）。
- 書き込み API が該当層のみを更新し、他層のファイルを触らないテスト。
- 保存失敗が通知として観測できるテスト。

### 単位 2 (`SET-2`): 設定ダイアログの骨組み

- `gpui_component::setting::Settings` でサイドバー + ページ構造を組む。
- `CommandId::AppPreferences` / `CommandId::ProjectSettings` を
  `for_each_command!` に追加し、メニューとキーバインド資産に載せる。
- 2 画面（環境設定 / プロジェクト設定）。この単位では**項目を 1 つも
  置かない**（骨組みだけ）。
- ロケール文字列（`menu.edit.preferences` ほか）を全ロケールに追加。

**完了条件**

- コマンドからダイアログが開く / 閉じるテスト。
- メニュー・キーバインド・コマンドテーブルが同じ Action を生成するテスト
  （`command_action_table.rs` の作法に従う）。
- ダイアログを開いている間もワークスペースの focus 所有権が壊れない
  （`FocusedPanelGlobal` が書き換わらない）テスト。
- 全ロケールに翻訳キーが存在するテスト（既存の i18n テストに乗せる）。

### 単位 3 (`SET-3`): 外観（テーマモードとテーマ選択）

- テーマモード: システム / ライト / ダーク。現状 `main.rs:114` の
  `sync_system_appearance` 固定を、設定値で分岐させる。
- テーマ選択: `ThemeRegistry::sorted_themes()` を scrollable_dropdown に。
  ライト用 / ダーク用を別々に持つ（現在 `"Ravel Light"` / `"Ravel Dark"` を
  ハードコードで拾っている箇所を設定値経由にする）。
- `watch_dir` によるホットリロードは既存の挙動を壊さない。

**完了条件**

- モードを切り替えると次の描画で配色が変わるテスト。
- テーマを選ぶと `Theme` の light / dark が差し替わるテスト。
- 設定に無いテーマ名が保存されていた場合のフォールバックのテスト。
- 「既定に戻す」で `global` 層の値が消え、システム追従に戻るテスト。

### 単位 4 (`SET-4`): 言語の切り替え UI

- `ravel_i18n::available_locales()` を dropdown に。
- 切り替え後、開いている全パネルが再描画される（`render()` で設定を書かず、
  購読側が `notify` する）。

**完了条件**

- 切り替えでパネルの文字列が変わるヘッドレステスト。
- 切り替えが `settings.toml` に永続化され、再起動後も維持されるテスト。
- 切り替え中にダイアログ自身のラベルも更新されるテスト。

### 単位 5 (`SET-5`): キーバインドのユーザー上書きと一覧

- 起動時に `<config>/keybindings.toml` を既定へ重ねて読む（`LOW-APP-15`）。
  パーサーは既存（`keybindings/parser.rs:71-146`）。
- 環境設定に**読み取り専用の一覧**（コマンド / 現在の割り当て / 由来が
  既定かユーザーか）。
- 不正な行は無視して警告（起動を止めない）。

**完了条件**

- ユーザーファイルが既定を上書きするテスト。
- ユーザーファイルが無い / 壊れている場合に既定で起動するテスト。
- 一覧が由来（既定 / ユーザー）を正しく表示するテスト。
- **`MED-APP-16` の回帰を作らない**こと（資産由来のバインドを context 無しで
  登録しない。フェーズ A の修正を前提にする）。

### 単位 6 (`SET-6`): プロジェクト設定画面

- `project` 層へ書く画面。**効く項目のみ** = 既定フレームレート
  （新規コンプ作成時の初期値に配線する）。
- 未保存プロジェクトでの扱い（保存時に一緒に書かれる）。

**完了条件**

- 既定フレームレートを変えると新規コンプの初期値が変わるテスト。
- **プロジェクト層の値がグローバル（`global` 層）を上書きするテスト**（層の
  向きを間違えると逆になるので、これを回帰として残す）。
- 「既定に戻す」でプロジェクト層の値が消え、グローバル値に戻るテスト。
- 未保存プロジェクトで設定してから保存すると `.ravprj` に入るテスト。

### 単位 7 (`SET-7`): 文書更新

- `docs/requirements/REQ-PROJ.md`: REQ-PROJ-004 を単一ファイル方式に改訂。
  「設定 UI で全設定を変更できる」を「**効く設定は UI から変更できる**」に
  改め、未対応の設定を明記。import / export の扱いを記載。
- `docs/requirements/REQ-UI.md`: キーバインド設定 UI の受入条件を「一覧」と
  「編集」に分割（本計画は一覧まで）。
- `docs/specifications/ui-spec.md`: 設定ダイアログの節を追加。
- `docs/implementation/backlog.md` / `roadmap.md` / `README.md`。

### ゲート付き後続単位（機能が入ってから実装する）

「出す項目 = 効く項目」規約により、これらは**前提機能のマージ後**に着手する。
計画としては今確定させ、着手条件だけを書いておく。

| ID | 項目 | 着手条件 |
|---|---|---|
| `SET-8` | キャッシュ（場所 / 各層の上限 / sim 予約率 / ディスク層の有無） | `cache-plan.md` の `CACHE-3`（`CacheBudget`）がマージされたら |
| `SET-9` | 自動保存（間隔 / 無効化） | REQ-PROJ-002 の自動保存タイマーが実装されたら |
| `SET-10` | プロキシ（`proxy_mode` / 解像度） | プロキシ生成（REQ-PROJ-005）が実装されたら |
| `SET-11` | カラー（OCIO / working / display space） | カラー管理（REQ-RENDER-003）が実装されたら |
| `SET-12` | キーバインドの割り当て編集（衝突検出 / chord 入力キャプチャ。REQ-UI-007） | 一覧（`SET-5`）の後。規模が大きいので別計画に切り出す判断もあり |
| `SET-14` | UI スケーリング（REQ-UI-006） | **調査が前提** — `Theme` は `font_size` / `mono_font_size` を持つが、Ravel のパネルが px 直書きでどれだけ無視しているか未調査。無視されるなら「効く項目」の条件を満たさない |
| `SET-15` | 色覚多様性テーマ / アニメーション削減（REQ-UI-006） | テーマ資産の追加と、アニメーションを持つ箇所の棚卸しが前提 |
| `SET-13` | 設定の import / export | 項目が揃ってから（今やると出力形式が毎回変わる） |

## 検証

- **単位 1 が UI 無しで完結すること**が本計画の骨。ここが入れば
  `MED-APP-10` は UI の有無に関係なく消える。
- 設定を変えた結果は**挙動でテストする**（dropdown の存在ではなく、
  文字列 / 配色 / 初期値が変わること）。
- ダイアログは `.agents/rules/gpui.md` の 3 点を明示的に検査する:
  `render()` に focus 変更とコマンド実行を持たない、`FocusedPanelGlobal` を
  奪わない、コマンド経路が `dispatch_command` に限られる。
- ロケール追加時の抜けは既存の i18n テストで捕まえる。

## 非対象

- **キー割り当ての編集 UI**（`SET-12`。一覧までが本計画）。
- **設定の import / export**（`SET-13`）。
- **プラグイン設定**（REQ-PLUGIN 系が未実装）。
- **UI スケーリング・色覚多様性テーマ・アニメーション削減**（`SET-14` /
  `SET-15`。REQ-UI-006 の残りの受入条件）。
- **カテゴリ別 5 ファイル構成**（要件を改訂して単一ファイルを正にする）。
- **`user` 層を UI から書くこと**（置き場も呼び出し元も無い。マシン固有の
  最終上書き用の枠として空けておく）。
- **分離ウィンドウ化**（`MED-APP-01` を前提にするので将来方向）。
- **設定を undo に載せること**。
