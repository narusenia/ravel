# Justified exceptions for scripts/lint-patterns.sh.
# Format: <rule> <file> <detail>
# Every entry needs a comment explaining why it is allowed.

# Durable shared state: Option<PanelKind> means "no panel focused", not a
# one-shot event. Kept in sync by GPUI focus events (Phase 3).
global-option-event crates/ravel-app/src/panels/mod.rs FocusedPanelGlobal

# The focus repaint follows durable state written by real focus events (the
# taxonomy-compliant use kept by Phase 5). Since the dock cutover the tab bars
# — where the focused pane is marked — are drawn by the window host, so one
# observer per window replaces the former one per panel.
observe-global crates/ravel-app/src/window_host.rs FocusedPanelGlobal
# Durable shared selection state: Viewer writes must repaint Node Editor node
# highlighting immediately (REQ-UI-011 unit 4). The same entry also covers the
# panel's test probe that counts `CanvasSelection` publications — the HIGH-07
# guard's contract is "an unchanged selection is not republished", and
# `set_global` wakes observers even for an identical value, so subscribing is
# the only way to observe it.
observe-global crates/ravel-app/src/panels/node_editor.rs CanvasSelection
# Test probe, not a signal path (same reason as the entry above): the
# rubber-band test asserts that a band move enclosing an unchanged node set
# republishes neither global. `notify_properties_selection` calls `set_global`,
# which wakes observers even for an identical value — the Properties panel then
# re-resolves every section — so subscribing is the only way to count the
# publications the guard removes.
observe-global crates/ravel-app/src/panels/node_editor.rs SelectedPropertiesTarget

# Durable shared UI state (REQ-UI-013): the Timeline mirrors the active
# composition and highlights the shared layer selection, both of which the
# Outliner and composition commands also write. Same lifecycle as
# CanvasSelection in the Node Editor.
observe-global crates/ravel-app/src/panels/timeline.rs ActiveComposition
observe-global crates/ravel-app/src/panels/timeline.rs LayerSelection
# Durable shared UI state (refactor-plan-0808 unit 8): the beat grid is written
# by the Timeline toolbar, by a project load and by File ▸ New, so a Timeline
# instance that did not do the writing still has to repaint from it. Same
# lifecycle and same reason as the two entries above.
observe-global crates/ravel-app/src/panels/timeline.rs BpmGridState
# Durable shared UI state (refactor-plan-0808 unit 9): the loop range is
# written by the Timeline ruler, by the playback commands, by a seek that
# leaves the range, and by a project load. The band must repaint from writes
# this Timeline instance did not make, so it observes the state for the same
# reason as the entry above.
observe-global crates/ravel-app/src/panels/timeline.rs LoopRangeState
# Durable shared UI state (parameter-groups-plan PGRP-3): which parameter
# groups are folded is written by whichever Properties panel the user clicked
# in, by a project load and by File ▸ New. A second Properties panel shows the
# same node type, so it has to repaint from a fold it did not perform — the
# same lifecycle and the same reason as BpmGridState above.
observe-global crates/ravel-app/src/panels/properties.rs CollapsedParamGroupsState
# Durable shared UI state (parameter-groups-plan PGRP-5): whether the node
# bodies draw their parameter rows is written by the View menu, by a project
# load and by File ▸ New — never by the panel itself. Every Node Editor
# instance (a detached window included) has to re-measure its nodes from a flip
# it did not perform, because `node_sizes` is what every hit test reads. Same
# lifecycle and same reason as CollapsedParamGroupsState above.
observe-global crates/ravel-app/src/panels/node_editor.rs ShowNodeParamValuesState
# The Node Editor opens the network of the shared layer selection (REQ-UI-013
# unit 3). Timeline and Outliner both write that selection, so observing the
# durable state replaces per-writer pushes into this panel.
observe-global crates/ravel-app/src/panels/node_editor.rs LayerSelection

# Outliner panel (REQ-UI-013 unit 3): the tree repaints on focus, mirrors the
# active composition and the shared layer selection it writes with the
# Timeline, and highlights the node selection it writes with the Node Editor.
# All durable shared state, same lifecycle as CanvasSelection.
observe-global crates/ravel-app/src/panels/outliner.rs ActiveComposition
observe-global crates/ravel-app/src/panels/outliner.rs LayerSelection
observe-global crates/ravel-app/src/panels/outliner.rs CanvasSelection
# A composition row's highlight IS the Properties composition target
# (REQ-UI-013 unit 4): the same durable state the composition commands read,
# so the tree repaints when it changes. Same lifecycle as the entry above.
observe-global crates/ravel-app/src/panels/outliner.rs SelectedPropertiesTarget

# Properties target identity is durable shared state; the panel resolves all
# values from the live document rather than storing snapshots in the Global.
observe-global crates/ravel-app/src/panels/properties.rs SelectedPropertiesTarget

# Viewer panel: reads durable ViewerFrame to display the current evaluation
# result — not a one-shot event, same lifecycle as SelectedPropertiesTarget.
observe-global crates/ravel-app/src/panels/viewer.rs ViewerFrame

# Properties panel: PlaybackPosition is durable shared playhead state (same
# kind as ViewerFrame); node-target sections resample animated channels at
# the playhead's layer-local frame when it moves (REQ-LAYER-004).
observe-global crates/ravel-app/src/panels/properties.rs PlaybackPosition

# The visibility gate (MED-UI-02): `VisiblePanels` is durable shared state —
# which panel instances are at the front of a tab area — written only by
# `WindowHost::show_tree`, exactly like `FocusedPanelGlobal`. The shared
# `on_became_visible` helper is the single observer of it, so every panel that
# delays its sync while hidden goes through this one subscription rather than
# declaring its own.
observe-global crates/ravel-app/src/panels/mod.rs VisiblePanels

# H-hold temporary hand tool (REQ-UI-011): raw key handling is the approved
# pattern for transient drag modes per .agents/rules/gpui.md.
panel-on-key-down crates/ravel-app/src/panels/viewer.rs on_key_down

# Inline layer rename (REQ-UI-013 unit 5): text entry is the other approved
# raw-key case. `InputState` emits no event for Escape, and its Enter action
# does not reach a subscriber from a row (the pre-existing Properties name
# field behaves the same), so the row confirms/cancels the edit itself.
panel-on-key-down crates/ravel-app/src/panels/outliner.rs on_key_down

# Inline asset rename in the MediaBin (`AID-3`): the same text-entry case as
# the Outliner row above, for the same reason — Escape has no event and the
# row's Enter never reaches a subscriber.
panel-on-key-down crates/ravel-app/src/panels/media_bin.rs on_key_down

# Viewer toolbar repaints when the active tool changes (durable shared state,
# same lifecycle as FocusedPanelGlobal / PlaybackPosition).
observe-global crates/ravel-app/src/panels/viewer.rs ToolState

# Viewer bbox overlay repaints when the canvas selection changes (durable
# shared selection state, REQ-UI-011 unit 3; same lifecycle as ToolState).
observe-global crates/ravel-app/src/panels/viewer.rs CanvasSelection

# Viewer layer bboxes and the multi-layer move gesture follow the shared layer
# selection (durable shared state, REQ-UI-013 unit 6; same lifecycle as
# CanvasSelection above).
observe-global crates/ravel-app/src/panels/viewer.rs LayerSelection

# The pixel readout's scale is durable shared state written by a command
# (`INSP-3`), and the Viewer is the only reader. Nothing else repaints for it:
# with the pointer standing still the chord would otherwise change the printed
# numbers only at the next frame. Same lifecycle as ToolState above.
observe-global crates/ravel-app/src/panels/viewer.rs ViewerReadoutFormat

# MediaBin panel (REQ-UI-008, media-import plan unit 4): repaint on focus
# change (same as every other panel), and highlight the shared media
# selection it writes through set_media_selection — durable shared state,
# same lifecycle as LayerSelection in the Timeline/Outliner.
observe-global crates/ravel-app/src/panels/media_bin.rs MediaSelection

# Node Editor per-node load readout: NodeEvalTimings is durable shared state
# (the latest evaluation's per-node durations, same lifecycle as ViewerFrame).
# `ProjectState::on_eval_update` publishes it and deliberately does not notify
# its observers, so the panel that draws it subscribes to it directly instead
# of every document-mirroring panel rebuilding once per evaluated frame
# (ui-responsiveness-plan.md RESP-1).
observe-global crates/ravel-app/src/panels/node_editor.rs NodeEvalTimings

# Attribute spreadsheet (REQ-CORE-010 inspection UI): the panel reads the same
# two pieces of durable shared state the Viewer overlays read. The selection
# names the node to inspect and changes with no evaluation behind it;
# `EvalResults` is the scoped-target results of the frame on screen, republished
# wholesale per evaluation, and `ProjectState::on_eval_update` deliberately does
# not notify its observers when it publishes it (the same reason
# `NodeEvalTimings` is on this list). Both are durable state with the same
# lifecycle as `ViewerFrame`, not one-shot events.
observe-global crates/ravel-app/src/panels/attribute_spreadsheet.rs SelectedPropertiesTarget
observe-global crates/ravel-app/src/panels/attribute_spreadsheet.rs EvalResults
# Same panel, same taxonomy: `ActiveComposition` is the durable "which
# composition is on screen" state. A switch withdraws the node target without
# writing either global above, so without this observer the sheet keeps the
# previous composition's rows until an evaluation happens to republish
# `EvalResults`.
observe-global crates/ravel-app/src/panels/attribute_spreadsheet.rs ActiveComposition

# Test probe, not a signal path: the RESP-2 regression test asserts that the
# Node Editor stops republishing an unchanged `CanvasSelection` on every graph
# change (HIGH-07). Counting publications is only observable by subscribing to
# the global — `set_global` wakes observers even when the value is identical,
# which is exactly the cost under test.
observe-global crates/ravel-app/tests/panel_rebuild_gate.rs CanvasSelection

# Durable shared state (the resolved settings, `SET-1`), and the one thing a
# repaint cannot reach: the platform menu bar is not part of any window's
# element tree, so the labels a language switch has to replace were baked into
# the `Menu`s handed to the OS. `AppSettings` is a Global rather than an entity,
# so there is no EventEmitter to subscribe to instead; the session observes it
# and rebuilds the menus (`SET-4`).
observe-global crates/ravel-app/src/workspace.rs AppSettings

# Durable shared state, and the only way to notice that the theme a settings
# file names has finally been loaded: the themes directory is read
# asynchronously and re-read on every file change, and gpui-component's own
# registry observer re-resolves the theme slots from the names the `Theme`
# already holds — after a fallback, the fallback's names. Observing the registry
# is what lets the requested theme be applied when it appears (`SET-3`).
observe-global crates/ravel-app/src/app_settings.rs ThemeRegistry

# Not FrameBuffer.data: AudioBuffer samples (audio_sample_format,
# integration_ffmpeg) and the FFmpeg AVFrame.data plane pointer array
# (decoder) keep their own representations and may be indexed directly.
framebuffer-direct-index crates/ravel-media/src/decoder.rs data-index
framebuffer-direct-index crates/ravel-media/tests/audio_sample_format.rs data-index
framebuffer-direct-index crates/ravel-media/tests/integration_ffmpeg.rs data-index
